# Copy this file to .env and adjust if your backend runs somewhere other
# than http://localhost:8000
VITE_API_URL=http://localhost:8000

# Must match the API_KEY environment variable set on the backend.
# Default "dev-secret" works with the backend's default for local dev.
# Change BOTH sides for any real deployment.
VITE_API_KEY=dev-secret
